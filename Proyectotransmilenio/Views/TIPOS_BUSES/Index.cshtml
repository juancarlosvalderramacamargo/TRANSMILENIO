@model IEnumerable<Proyectotransmilenio.Models.TIPOS_BUSES>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo Tipo de Bus", "Create")
</p>
<table class="table">
    <tr>
        <th>
            TIPO BUS
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_BUS)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_TIPO_BUS }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ID_TIPO_BUS }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_TIPO_BUS })
        </td>
    </tr>
}

</table>
