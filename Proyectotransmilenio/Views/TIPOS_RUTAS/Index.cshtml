@model IEnumerable<Proyectotransmilenio.Models.TIPOS_RUTAS>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo Tipo de Ruta", "Create")
</p>
<table class="table">
    <tr>
        <th>
            TIPO RUTA
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TIPO_RUTA)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_TIPO_RUTA }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.ID_TIPO_RUTA }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.ID_TIPO_RUTA })
        </td>
    </tr>
}

</table>
